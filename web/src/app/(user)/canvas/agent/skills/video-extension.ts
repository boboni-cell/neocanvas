export const VIDEO_EXTENSION_SKILL = String.raw`
【视频续写规则】

## 1. 适用与前提

适用：继续、延长、下一段、发生在之前、补拍、连续片段、一镜到底。
前提：必须有真实成功的视频来源；确认当前模型支持视频参考；不支持时给出替代方案但不冒充续写。

## 2. 续写方向与模式

### 向后续写（默认）

“从视频1硬切到同一世界的新摄影机角度；不要匹配或复制视频1的最后一帧。”

### 向前续写（前传）

“片段发生在视频1之前。角色、地点和背景从图片参考继承。保持连续性，结束时导向视频1的开场状态。”

### 同机位连续

仅用于：必须一镜到底、用户明确要求、稳定停顿或固定讲台/采访。

“从视频1最后动作之后继续。保持机位、构图、地点和光线不变。不重复视频1任一帧。”

## 3. 连续链规则

- 每个新片段只使用直接前序视频作为视频来源。
- 始终延续使用角色/产品/场景正式参考。
- 每段只写本段台词和动作，不重复已说完内容。
- 边界默认硬切新角度。
- 对白/旁白保留到匹配片段。

## 4. 连续链规划

先确认每段合法 seconds 和总片段数：

“连续链计划：N 段，每段约 X 秒。片段1→硬切→片段2→……；角色/产品/场景参考在每段中保持。”

硬切边界默认：锁定地点/光线/服装/产品/角色身份；改变摄影机角度/构图和新动作；不匹配上一机位或最后一帧。

同机位边界：锁定地点/光线/摄影机位置/构图/姿态/服装/产品；改变动作和时间推进。

## 5. 边界提示词模板

### 硬切

“[新动作描述]。图片1中的角色、图片2中的产品和图片3中的地点保持。从视频1硬切到新角度；不匹配视频1的最后一帧……”

### 同机位

“从视频1最后动作之后继续。[后续动作]。保持机位、构图、地点和光线。不重复来源帧……”

### 前传

“发生在视频1之前。[动作描述]。角色、地点和背景从图片参考继承。结束时导向视频1的开场状态……”

## 6. 问题修正

- 边界融化/扭曲→改硬切新角度。
- 重复来源画面→强化“不复制视频1任何帧”。
- 身份漂移→补正确参考并在新片段重新绑定。
- 场景不连续→补详细地点参考。
- 对白重复→每段只写本段台词。
- 引用超限→每段只保留直接前序视频和必要锚点。

### 中间状态分支

用户从视频中间状态分叉时：用目标帧图片走图生视频；或先生成新关键帧再建立分支。分支连接真实来源视频、关键帧和创作文本。
`.trim();