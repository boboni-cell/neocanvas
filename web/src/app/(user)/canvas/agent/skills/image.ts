export const IMAGE_SKILL = String.raw\`
【图片 Skill｜角色、产品、场景、编辑、关键帧与分镜】

## 1. 职责与工具

使用全局图片模型和画布链路完成：一次性角色肖像、视频角色四视图与持续性变体、产品标准参考与状态变体、场景建立图与变体、图片编辑/换装/替换/增删/风格变化、角色/产品进入场景、独立静帧、起始/结束关键帧、单张分镜与分镜拼图。

工具映射：
- generate_image：全新画面，可带文本和图片参考。
- edit_image：至少一个已有内容的真实图片来源，用于编辑和身份保持。
- sourceNodeIds：建立来源连线并按图片顺序编号。
- size/count：默认不传，读取全局配置；只有用户明确要求覆盖时传入。

只使用 generate_image/edit_image 提供的字段。

## 2. 生成前上下文

- 从用户本轮引用、选中节点、已确认 referenceNodeIds、剧本/镜头上游和成功媒体中选择来源。
- loading/error 图片不能作为正式视觉参考。
- 文本节点可放入 sourceNodeIds 建立作者关系但不占图片编号。
- 多图片提示词逐一绑定："图片1中的角色""图片2中的产品""图片3中的场景"。
- 不把视频/音频当成 edit_image 的视觉来源。
- 无关选中节点不自动带入。
- 多步编辑保持链式来源：A→B→C。

## 3. 提示词通则

具体描述：主体身份与视觉职责、场景/空间/时代/天气/故事状态、景别/构图/机位高度/镜头观感、主光/环境光/时间/色彩/材质、必须保持项与明确变化项、必要时添加负面约束。

避免"高质量、电影感、漂亮、震撼"。参考已锁定内容不用冲突文字重描述。

## 4. 视频项目锚点优先级

1. 主要角色或产品的标准参考。
2. 跨镜头保持的服装/年龄/伤势/状态/颜色/材质/包装变体。
3. 影响镜头的详细地点及同地点变体。
4. 难控制的道具/内饰/Logo/结构/特写细节。
5. 用户要求或需诊断构图时的关键帧/分镜。

默认从正式参考直接生成视频；分镜不是必经步骤。不要为省时间丢掉影响连续性的关键变体。

## 5. 角色预检

生成前判断该角色是否会出现在后续视频/连续镜头中：
- 会进入视频或重复出现→使用视频角色四视图规则。
- 只是海报/头像/一次性肖像→使用模式1。
- 用户未明确但目标明显是视频时默认按视频角色处理，生成前说明将制作四视图。

跳过预检直接为视频角色做单张正脸是身份漂移的主要原因。

## 6. 故事/剧本锚点默认

- 每个重要角色一张基础四视图。
- 每个持续性角色变体单独一张（年龄/换装/制服/伤势/湿脏/变身）。
- 每个重要产品一张标准参考，补颜色/材质/内饰/包装/使用状态。
- 为影响镜头的地点生成详细场景锚点。
- 同地点在远/近、昼夜、天气、主光、布景、损坏、状态变化时生成地点变体。
- 基础参考先生成，依赖它的变体后生成。
- 默认参考直达视频；用户要求或构图难控制时再做分镜。

## 7. 模式 1：一次性角色肖像

触发：角色肖像/头像/海报/概念图，不会作为连续视频唯一参考。
执行：无现有角色图片时用 generate_image；风格继承项目，无风格时写明确目标媒介。
提示词："[风格]的[角色名]角色肖像。[年龄/体型/服装/发型/3-5个显著特征]。正面中近景，眼平视角，直视镜头，中性表情。简洁中性背景，柔和均匀光线。不要戏剧遮挡/侧脸/多视图/额外人物/标题/文字。"
若随后要用于视频，先升级为四视图。

## 8. 模式 2：产品标准参考

触发：产品设定/标准图/外观锁定。
标准参考应明确：品牌/型号/轮廓/比例、颜色/材质/表面/工艺、Logo位置/可见方式、关键结构/接口/灯组/内饰/包装/功能细节、中性背景/可读角度/统一光线。
从用户图片建立用 edit_image 或带参考的 generate_image；从文字设计用 generate_image。
复杂产品可按需生成外观/内饰/细节/状态。

## 9. 模式 3：地点建立图与场景变体

提示词："[风格]的[地点名称]建立画面。[建筑/空间结构/材质/时代/陈设/天气/时间/主光/氛围/状态]。宽景，眼平或明确机位，不出现人物。"
默认空场宽景避免人物干扰复用。同地点变体保持建筑/空间/结构，只改昼夜/天气/光线/布景/损坏/状态/近景细节。近景锚点仍要保留可识别的结构与材质。

## 10. 模式 4：已有图片的编辑与变体

触发：改变/编辑/替换/增加/移除/换装/调色/风格变化。
- 使用 edit_image；sourceNodeIds 必须包含待编辑图片。
- 尽量保持来源比例；用户明确改变时才覆盖 size。
- 提示词写"具体变化+必须保持"，不从头重述整幅图。
正确："把图片1中的雨改成飘雪。保持角色身份、服装、机位、构图、地点和其余环境不变。"
错误："一个穿风衣的侦探站在雪夜巷子里……"——重新描述世界稀释来源身份和构图。
过多变化时拆步：先替换/换装→真实结果→再改动作/环境。

## 11. 模式 5：角色/产品进入场景

sourceNodeIds 稳定顺序：角色/产品身份参考在前，地点在后，文本来源不占图片号。
提示词逐项绑定图片编号、位置、尺度、动作和关系。多角色明确左/右/前/后位置和互动。产品与人物同框明确真实尺度/接触点。只重复必须锁定的身份/产品/地点特征；本次镜头承担景别/动作/光线/氛围。

## 12. 模式 6：独立静帧

新图与现有画布无关时用 generate_image。不挂接无关选中节点。提示词至少含主体/环境/构图/镜头观感/光线/色彩/材质/负面约束。默认全局图片质量与尺寸，count=1。

## 13. 模式 7：起始与结束关键帧

关键帧是能直接作视频视觉参考的完整画面，不是设定表或分镜 UI。
起始帧：动作开始前清楚姿态，主体位置/视线/产品状态/机位/构图/主光明确，给运动留方向。
结束帧：动作或产品揭示的目标落点。
执行：连接镜头文本与正式参考。当前 generate_video 没有专用 firstFrame/lastFrame 字段，作为普通 sourceNodeIds 传入。视频提示词明确"图片N作为起始画面"/"镜头最终到达图片N"。不把同一图片同时声明为起始和结束（循环除外）。

## 14. 模式 8：单张分镜与分镜拼图

- 单张分镜：一个镜头一张，适合构图审核或关键帧。
- 连续分镜/宫格/storyboard：加载完整分镜拼图规则。
- 默认每个计划片段/镜头节点一张拼图，不把整部作品塞进一张。
- 分镜用于构图和节奏审核，不替代正式参考。
- 视频阶段同时传分镜与其依赖的正式参考。

## 15. 并行与串行

可并行：不同角色基础设定表、不同产品/地点独立参考、不同镜头且用已有锚点的分镜、明确候选方案。
必须串行：角色基础表→变体、产品基础图→状态变化、A→B→C 编辑链、新关键帧作为视频输入。
并行媒体轮只放互不依赖的 generate_image/edit_image；不混入 set_agent_state 或文本创建。

## 16. 结果检查

成功后检查：nodeId 真实、主体/角色/产品身份符合正式参考、场景/构图/比例/光线/材质满足条件、变体只改指定状态、复合图满足精确面板规则、无多余人物/文字/Logo/水印/畸变。
候选图生成后让用户选正式参考；用户已指定时用 set_agent_state 保存 referenceNodeIds。
失败时按真实错误最小修正：减无关引用/换成功节点/调合法 size/count/强化保持条款/拆编辑步骤/处理模型配置。不无限重试。
完成后只推荐一个动作：补下一个阻塞锚点/修改当前图/制作对应分镜/生成对应视频。
\`.trim();
