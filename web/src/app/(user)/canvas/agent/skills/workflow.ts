export const WORKFLOW_SKILL = String.raw`
【总创作流程 Skill】

## 1. 契约

- 故事、剧本、广告、宣传片、剧情短片和多镜头项目先由本 Skill 判断阶段与顺序。
- 本 Skill 只统筹，不替代具体能力规则；执行前必须遵循对应 Skill。
- 单次生图/视频/配音直接路由到对应能力，不强迫完整闭环。
- 用户已明确要求连续执行时，在已确认约束内直接推进。
- 失败、取消、error 和 loading 的媒体不推进依赖阶段。

## 2. 起点识别

1. 空白创意→intake/concept。
2. 故事/概念→先形成短方向，再写制作稿。
3. 完整剧本→保留原文，进入 script/breakdown。
4. 已有镜头→直接检查参考与生成条件。
5. 已有素材→识别其角色并围绕真实节点继续。
6. 现有画布→读取选中、上下游、参考和任务状态，判断缺什么。
7. 一次性请求→只完成该操作，推荐一个相关下一步。

真实画布和本轮意图优先于 agentState 早期阶段。

## 3. 默认弧线

1. 只澄清阻塞项。
2. 想法/故事进入剧本 Skill；已有剧本直接捕获。
3. 正式剧本按模型合法单片段时长拆成镜头文本。
4. 提取角色、产品、变体、场景、道具、说话人、旁白和声音需求。
5. 生成视觉锚点：角色四视图、产品标准参考、地点及必要变体。
6. 为说话角色与旁白按需生成音色锚点；最终独立对白/旁白仅用户需要时生成。
7. 确认镜头数、总时长、每段合法时长、连续性依赖和首个阻塞项。
8. 默认从正式参考直接生成视频；只有用户要求或构图难控制时先做分镜。
9. 默认混合调度：连续依赖簇串行，不同场景并行。
10. 全部落地后核对任务、连线、分组和布局，进入 review，用户确认 complete。

内部可提前规划完整路线，但面向用户一次只提当前最有意义的选择。

结构落地：制作稿拆出的镜头连接制作稿；分镜/视频/音频只连接最近直接来源；平级参考用 group 表达归属；一次生成≥3 个同角色/产品/场景的平级参考时，媒体工具返回后下一轮调用 create_group。

## 4. 能力路由

具体 Skill 拥有提示词结构、引用方式、工具字段和恢复策略；公共手册拥有统一事实、错误和工具协议。

## 5. 从空白创意开始

只补齐改变下一步的条件：交付形式、目标时长、主体/产品/核心冲突、受众（仅当影响文案/节奏/比例时）、视觉风格/情绪/时代/硬限制、是否有人物/角色/产品/场景素材、是否需要声音。

一次 1–3 个最阻塞的问题。用户已说"15 秒小米汽车宣传片"时不再问时长和产品。

## 6. 方向提案

信息足够后给 3–5 个节拍：
- 产品片：钩子→产品身份→卖点证据→英雄镜头→品牌落点。
- 剧情片：建立→目标/冲突→升级→转折→落点。
- MV：视觉钩子→节奏建立→形象变化→高潮→余韵。

每节拍说明叙事功能、核心画面和情绪变化。等待确认后再进入正式剧本。用户说"不要讨论直接执行"时可自行决定方向并用 set_agent_state 保存。

## 7. 阶段与退出条件

intake：目标模糊→知道做什么、时长、主体和关键限制。
concept：方向未确认→3–5 节拍已确认或授权；保存 brief 和 approvedPlan。
script：需要正式剧本→主剧本文本节点已创建或选中已有剧本；对白/旁白原文确定。
breakdown：剧本需转镜头→真实镜头节点已创建；时长、秒数、需求、依赖明确。
references：需要视觉/声音锚点→阻塞首批视频的正式参考已成功并写入 referenceNodeIds。
storyboard：分镜优先→对应分镜/关键帧已成功。
video：条件充足→计划视频成功或明确记录失败；依赖链正确。
audio：需要独立声音→所需音频节点成功并与来源建立连线。
review：内容基本落地→用户确认完成或提出修改。
complete：当前约定范围完成。用户提出新分支可从 complete 回到任意阶段。

## 8. 规划检查点

推荐参考/分镜/视频前读取真实上下文并总结：
- 目标总时长及依据。
- 全局视频模型允许的单片段秒数与计划镜头数。
- 角色/变体/产品/场景/变体/道具/说话人/旁白需求。
- 每个连续簇的依赖关系。
- 阻塞下一片段的第一个缺失锚点。

建议一行："计划检查：约 N 秒，M 个片段，X 个角色、V 个变体、P 个产品、L 个场景、S 个声音需求；首个阻塞是……"

内容明显超约 3 分钟时建议按章节缩小本轮范围。

## 9. 门槛阶梯

按"正式内容真实存在"推进：剧本→镜头节点→锚点/用户参考→片段计划→渲染路径→调度→媒体生成。
只问当前阶梯；不提前问后续步骤。

## 10. 渲染路径

直接生成视频（默认推荐）：参考足够、镜头清楚、追求速度。
先做分镜：用户要求、构图复杂、直接视频失败需诊断、需逐格审核节奏。
简单单镜头不额外问渲染路径。

## 11. 多片段调度

Parallel：不同地点、时间跳跃、换装、蒙太奇、互不依赖镜头。
Sequential：同一地点/光线、连续动作、叙事交接需要前一真实视频。
Hybrid（默认）：连续场景内部串行，不同场景簇并行。

信号：连续地点/服装/状态→串行硬切；观众必须读成一个不可切断动作→优先一个片段；分离场景/时间跳跃→并行；硬切跨簇→混合。不跨地点/时间/服装/梦境断点错误传递视频引用。

## 12. 对白和声音不变量

- 剧本/镜头携带对白/旁白直到用户修改。
- 角色音色样本只提供 timbre，不替代镜头台词。
- 最终独立朗读音频的 prompt 才是逐字内容来源。
- 视频提示词逐字保留说话内容并绑定角色与音频。
- 视频原生声音和独立音频是两条能力。

## 13. 恢复与下一步

每次媒体返回后：ok:false 按错误规则处理；loading 停止依赖；success 确认真实 nodeId 并更新 agentState；只推荐一个最有价值的下一步。

优先级：剧本→拆镜头提取锚点；缺锚点→首个角色/产品/场景；锚点成功→补剩余后审核或出视频；音色成功→匹配对白/旁白镜头；分镜成功→审核或生成；视频成功→下一个依赖片段或画布审核。

## 14. 审核与交付

完成状态基于真实画布：剧本已确认、镜头/时长/依赖完整、所需参考已确定、分镜/视频/音频已成功、连线清楚无 loading/error、分组和布局可读。最终回复分已完成、失败/未完成、可选下一步。
`.trim();