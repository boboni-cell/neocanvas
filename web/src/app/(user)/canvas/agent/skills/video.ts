export const VIDEO_SKILL = String.raw\`
【视频 Skill｜通用规则、引用职责、模式与分镜保护】

## 1. 当前工具与字段

工具：generate_video（所有视频创建的统一入口）。
字段：prompt（仅视频提示词）、title（≤32字符或从 prompt 截取）、sourceNodeIds、size（默认不传，读取全局）、seconds（默认不传，读取全局，必须等于实际片段时长）、generateAudio（默认不传，读取全局）。

sourceNodeIds 同时建立真实来源连线并按顺序编号（图片1、图片2……视频1、视频2……音频1、音频2……）。独立片段不附加无关节点。

不发明工具未提供的字段。loading 和 error 不是正式媒体参考。

## 2. 模型能力预检

视频生成前先确认 get_generation_config 返回的当前模型能力：
- 图片参考是否支持及数量上限。
- 视频参考是否支持及数量/时长上限。
- 音频参考是否支持。
- 合法片段秒数/范围。
- videoGenerateAudio 是否支持。

不凭模型名称猜测；以工具返回和校验信息为准。

## 3. 片段时长

seconds 必须等于 prompt 描述的内容时长，且在当前模型合法范围内：
- 模型给出合法范围时，按用户目标选一个明确值。
- 只允许固定秒数时使用其中之一。
- 不要跨镜头时写"5秒推进+5秒环绕"等于10秒。

每个 generate_video 的 seconds = 片段内容总秒数；不存在默认时长。内容超出合法范围时拆成多个 video 节点或减少内容。

## 4. 视频提示词通则

提示词只描述最终观众可见/可听的内容，不描述编辑过程或"这个项目要做什么"。精修片段使用完整单镜头或多镜头结构；简单请求可用自然语言。具体描述：主体/动作、场景/光线/氛围、运镜方式（每个片段只一个主要运镜）、色彩/材质/效果、必要保持和变化项。

避免"高质量、电影感、震撼"等空泛词。参考锁定的身份和外观不写冲突版本。

## 5. 引用职责与编号

sourceNodeIds 分三步：确认真实引用节点→按类型和角色编入数组→prompt 逐项绑定职责。

编号：图片从1开始、视频从1开始、音频从1开始，各自独立。prompt 中每个编号必须有对应真实节点。职责写明：角色身份/产品外观/场景/起始画面/结束落点/视频续写来源/视频变换来源/运镜来源/动作来源/音色样本/最终朗读。

一个节点可以有多个职责但必须一致。只附真正需要的引用；无关引用稀释注意力。loading/error 不作为正式参考。引用上限以 Provider 返回为准。

## 6. 引用角色词汇

- 角色身份："图片1中的角色保持同一张脸、体型、服装和持续状态。"
- 产品："图片2中的产品保持轮廓、颜色、材质、结构、比例和 Logo 位置。"
- 地点："地点与图片3一致，保持空间结构、材质、时间和主光方向。"
- 起始画面："图片4作为起始画面；从该姿态和构图开始，然后……"
- 结束落点："镜头最终到达图片4展示的构图和主体状态。"
- 视频续写（默认硬切）："从视频1硬切到同一世界的新角度；不要匹配或复制最后一帧。"
- 同机位："从视频1最后动作之后继续；保持机位、构图、地点和光线，不重复来源帧。"
- 视频变换："重绘视频1为……；保持构图、运动和主体。"
- 运镜："仅借用视频1的摄影机运动节奏，不复制其主体、地点或动作。"
- 动作："动作编排参考视频1，但角色身份、服装和地点由图片参考决定。"
- 音色样本："音频1作为音色参考；图片1中的角色逐字说：'……'；只说一次，不回声，不重复。"
- 最终朗读："使用音频1的文字、时序、节奏、语气和音色，词句保持不变。"

不把同一图片同时定义为开场和结尾（除非循环）。没有专用 firstFrame/lastFrame 字段，都作为普通图片参考通过 prompt 解释。

## 7. 提示词语言不变量

- 对白/旁白逐字保留：不总结/翻译/润色/缩短/增加。
- 明确说话人：写"图片1中的角色说"，不写"图片1说"。
- 多角色逐一绑定图片与音频并明确台词顺序。
- 音色样本只锁定 timbre，不替代当前镜头台词。
- 最终朗读使用其 prompt 正文作为事实源。
- 音频上传无文字时只作声音/时序参考，不发明 transcript。
- 逐字朗读的模型添加："逐字说一次，不回声，不重复朗读。"易吞字名词可加发音提示但正文不变。
- 一个单镜头只使用一个主要运镜和一种明确动作速度。
- 使用准确术语：固定机位、轻微手持、缓慢推进、缓慢环绕、快速摇镜、速度渐变。
- 不写冲突的"固定+环绕""极慢+高速"。
- 写具体声音：环境声、动作音效、对白、旁白、音乐；不需要音乐写"无音乐"。
- 结尾添加：无字幕、无水印、无畸变、无拉伸、无额外文字/Logo。

## 8. 模式 1：独立文生视频

触发：与画布现有媒体无关的新片段。简单请求用自然语言；精修片段加载单镜头结构。sourceNodeIds 只含真正作者文本。视频配置使用全局设置。没有参考时不写图片N/视频N/音频N。

## 9. 模式 2：让画布图片动起来

触发：动画化、让这张图动、图生视频。使用真实成功的图片节点。分镜拼图走分镜保护（不当普通开场帧）。普通图片默认起始画面。sourceNodeIds 含目标图片、镜头文本和必要正式参考。不把图片锁定内容重描述成冲突版本。

## 10. 模式 3：角色、产品和地点合成

触发：角色在地点行动、人物使用产品、多角色对话、产品广告镜头。使用当前镜头的准确角色状态/变体，不盲目用最早基础表。使用正确型号/颜色/材质的产品参考。使用当前地点变体（昼夜/天气/主光/故事状态）。prompt 按图片顺序逐项绑定。多角色指定位置/动作/视线/互动/说话顺序。参考锁定身份/产品/地点；prompt 承担动作/景别/运镜/节奏/氛围/声音。

## 11. 模式 4：视频续写、前传和连续链

必须有真实成功的视频来源并确认模型支持视频参考。默认向后续写硬切新角度；向前补拍通过 prompt 描述。同机位仅用于稳定停顿或一镜到底。链式边界默认硬切。完整决策和限制遵循视频续写参考。

## 12. 模式 5：视频编辑

必须有真实成功的视频来源并确认模型支持视频编辑。根据意图区分 Restyle/Partial/Replace/Re-plot。源视频提供构图/运动/主体；prompt 只写变化和保持项。不支持时给出重建方案但不称编辑。完整模板遵循视频编辑参考。

## 13. 模式 6：声音驱动视频

### 音色样本
prompt 写当前已确认台词；图片绑定说话人，音频锚定声音；加逐字一次/无回声/无重复。

### 最终朗读
逐字使用音频节点 prompt 正文；写"使用音频N的文字、时序、节奏、语气和音色，词句不变"。

### 无音频节点
支持原生声音时按文字生成对白/旁白；不能声称复刻不存在角色音色。

### 音频不被当前模型支持
不附加音频冒充生效；可用文字+原生声音或保留独立音频供后期。

## 14. 模式 7：单片段多镜头、广告、品牌片和 MV

触发：一个合法片段内两个以上镜头/节拍或广告/品牌片/MV/产品片需要明确内部时间线。片段总内容必须在合法 seconds 内。从镜头节点提取时保持顺序和原文对白/旁白。从分镜拼图提取时每面板成为一个时间块，不渲染网格 UI。使用\"镜头时间线、效果清单、密度图、能量弧/叙事弧\"结构。内容超单段能力时拆成多个 video 节点。

## 15. 分镜拼图保护

检测到分镜拼图标题或用户说分镜/宫格/storyboard 时：
- 不写"图片N作为开场帧"。
- 写"根据图片N中的分镜面板按左到右、上到下生成正常视频"。
- 网格/黑色分隔线/数字徽标/版面不进入视频。
- 同时传入拼图上游的角色/产品/地点正式参考。
- 按面板计划分配实际 seconds；太短时拆视频或减明确子集，不擅自丢面板。
- 完整转译遵循分镜拼图与视频多镜头参考。

## 16. 常见组合

- 角色+声音→模式6；角色图片+对应音频。
- 带角色 MV→多镜头规则；角色图片+音乐/声音。
- 保身份换风格→视频编辑 Restyle；源视频+角色图片。
- 连续多片段→续写链；每段引用前序视频+角色/产品正式参考。
- 借鉴运镜→单镜头规则；主体图片+运镜视频（明写只借运镜）。
- 从一个镜头文本生成→按内容选模式；文本节点为来源。
- 连续剧本超单片段→续写链；硬切默认。
- 短剧本在单片段→多镜头规则。
- 分镜拼图动画化→多镜头规则+原始参考。

## 17. 并行、串行与混合调度

可并行：不同地点、时间跳跃、换装/状态变化、蒙太奇、备选方案、使用已成功共同参考的独立镜头。
必须串行：后一片段需要前一真实视频 sourceNodeIds；同一地点/人物/光线/叙事交接；续写链；依赖新关键帧/最终音频的片段。
混合：连续簇内部串行，独立簇之间并行。独立视频批次同轮只调用多个 generate_video；loading 结果停止依赖步骤。

## 18. 结果与保存

成功后保留真实 nodeId/createdNodeIds/connectionIds/taskId/status。实际参数由现有节点元数据/链路保存。generateAudio=true 的声音在 video 音轨内不额外创建 audio 节点。需要恢复时按 taskId 读取，禁止重复提交。

## 19. 视频错误修正

- unsupported_duration→改用合法秒数重新规划。
- video_audio_not_supported→关闭 generateAudio 或走独立音频。
- model_not_configured→提示用户完成配置。
- 引用内容读取失败→替换为真实可用参考。
- 引用数量超限→保留最重要参考，以 Provider 上限为准。
- 音频不能作为唯一参考→增加角色/场景图片或视频来源。
- 时长不合法→严格按照返回限制减少或更换。
- 视频引用总时长超限→减少视频参考，续写链只保留直接前序。
- 身份漂移→补正确角色四视图/产品标准图，删除冲突重描述，每段重新绑定。
- 运镜错误→消除冲突，只保留一个主要运镜。
- 对白重复/回声→强化"逐字说一次，不回声，不重复"，检查每角色绑定。
- content_filtered/rate_limited/timeout→按公共手册处理，不自动无限重试。
- 模型不支持视频参考/编辑→停止该路径，不冒充成功。
\`.trim();
