export const IMAGE_STORYBOARD_SKILL = String.raw\`
【分镜拼图规则】

## 1. 适用与跳过

适用：用户要求分镜/storyboard/宫格/mosaic/连续关键帧；需要一张图片预览多镜头节奏；多镜头内部构图诊断；每个计划片段一张拼图作为视频生成前审核。

跳过：单张分镜（直接用图片主 Skill）；独立关键帧（用图片主 Skill 起始/结束帧模式）。

## 2. 拼图规格

默认每个计划视频片段或合法时长镜头节点一张拼图。面板数 = 用户明确数或该片段内部镜头/节拍数。

面板布局：
- 2→1×2 或 2×1。
- 3→1×3 或 3×1。
- 4→2×2（默认首选）。
- 5→1×5 或 2×3（留空最后一格）。
- 6→2×3。
- 7-9→3×3。
- ≥10→建议拆分多张。

4×4 是单张可读上限；超过时先警告并建议拆分。

## 3. 比例与尺寸

拼图需容纳等宽面板，默认使用当前 Agent imageSize。竖向面板多的拼图可用 3:4 或 9:16。不确定时读取已有内容判断。

## 4. 拼图提示词结构

1. 格线规格：明确行列、黑色分隔线粗细、可选的左上角数字徽标。
2. 面板列表：每格从左到右、从上到下写出内容（景别/主体/动作/氛围），与镜头文本一致。不裁切原文对白。
3. 全局规则：统一风格/光线/色彩。明确无额外文字、Logo、水印、畸变。黑色分隔线和数字徽标仅用于阅读指导。

## 5. 来源与连线

从项目正式镜头节点构建拼图：
- sourceNodeIds 含所有对应镜头文本节点 + 已存在的角色/产品/场景正式参考。
- 保持镜头原文对白、动作和连续标注。
- 不添加镜头文本中没有的额外人物或事件。

## 6. 动画/角色连续拼图

连续动作或同一角色多个镜头时：
- sourceNodeIds 包含角色四视图设定表。
- 为多镜头保持固定脸、体型和服装。
- 各格仅改变景别、构图、动作和光线。

## 7. 产品/场景拼图

- 产品多角度/功能展示拼图：sourceNodeIds 含产品标准参考。
- 场景同地点多状态/时间拼图：sourceNodeIds 含场景建立图。
- 保持产品/地点身份和结构不变。

## 8. 生成后检查

- 面板数与布局是否匹配要求。
- 分隔线是否清楚、每格内容是否可读。
- 角色身份/产品外观各格是否一致。
- 是否有镜头的对白/动作/连续性标注被遗漏。
- 是否有畸变或面板混乱。

失败修正：格子被合并→强调\"等宽网格，每格独立隔离，不合并\"; 身份不一致→补充四视图并加强各格保持脸/体型/服装; 面板数错误→检查行列与镜头数是否匹配; 畸变→增加完整全身比例约束。

完成后不推荐其他动作，只推荐\"分镜拼图可用于审核构图的镜头，或直接送入视频生成\"。
\`.trim();
